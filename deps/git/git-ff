#!/bin/bash

# Usage: git-ff
#
# Fast-forwards the current branch without fetching.

remote_branch=$(
  git branch -vv \
  | grep -F '*' \
  | awk '{print $4}' \
  | perl -pe 's/\[(.*)\]/\1/g'
)
git merge $remote_branch --ff-only
